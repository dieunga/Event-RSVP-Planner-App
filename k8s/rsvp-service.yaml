apiVersion: apps/v1
kind: Deployment
metadata:
  name: rsvp-service
  namespace: soiree
  labels:
    app: rsvp-service
    version: v1
spec:
  replicas: 2
  strategy:
    type: RollingUpdate
    rollingUpdate:
      maxSurge: 0
      maxUnavailable: 1
  selector:
    matchLabels:
      app: rsvp-service
  template:
    metadata:
      labels:
        app: rsvp-service
        version: v1
    spec:
      containers:
        - name: rsvp-service
          image: <AWS_ACCOUNT_ID>.dkr.ecr.ap-southeast-1.amazonaws.com/soiree/rsvp-service:latest
          ports:
            - containerPort: 3003
          envFrom:
            - configMapRef:
                name: app-config
            - secretRef:
                name: app-secrets
          env:
            - name: PORT
              value: "3003"
          resources:
            requests:
              cpu: 100m
              memory: 128Mi
            limits:
              cpu: 250m
              memory: 256Mi
          readinessProbe:
            httpGet:
              path: /health
              port: 3003
            initialDelaySeconds: 10
            periodSeconds: 5
          livenessProbe:
            httpGet:
              path: /health
              port: 3003
            initialDelaySeconds: 15
            periodSeconds: 10
---
apiVersion: v1
kind: Service
metadata:
  name: rsvp-service
  namespace: soiree
  labels:
    app: rsvp-service
spec:
  selector:
    app: rsvp-service
  ports:
    - name: http
      port: 3003
      targetPort: 3003
      protocol: TCP
  type: ClusterIP
